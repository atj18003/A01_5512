# A01_5512
A01 - first assignment

Data used: California Housing dataset

Run script by
1. installing requirements via command "pip install -r requirements.txt"
2. changing working directory to A01 repo via command "cd path/to/A01"
3. running boxplot.py script via command "python src/boxplot.py" 

Output should be in figs folder